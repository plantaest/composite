/**
 * Shared user contract.
 *
 * The first milestone reserves this interface for future user capabilities.
 */
// biome-ignore lint/suspicious/noEmptyInterface: The first milestone explicitly defines User as a placeholder interface.
export interface User {}
