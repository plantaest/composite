Read `COMPOSITE.md` and all files under `docs/` before answering.

Your task is to review the current Composite documentation as a project reviewer, not as an implementation agent.

Focus on:
1. contradictions between files
2. hidden scope creep
3. unclear API semantics
4. places where the documentation leaves too much room for accidental implementation drift
5. assumptions that should be made explicit for the MediaWiki client runtime

Do not edit files yet unless explicitly asked.
Return:
- a concise summary of the project as you understand it
- a list of documentation problems ordered by importance
- specific proposed edits or follow-up questions
