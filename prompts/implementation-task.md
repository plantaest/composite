Read `COMPOSITE.md` and the relevant files under `docs/` before making changes.

Implement only the requested scope. Do not expand Composite v1 beyond the documented requirements.

Before editing code:
1. summarize the intended behavior based on the docs
2. list any ambiguities or missing information
3. propose a minimal implementation plan

If the docs are insufficient or contradictory, stop and say so instead of inventing major behavior.

When editing:
- keep changes small and reviewable
- align code with the documented API shape
- update documentation if public behavior changes
- avoid speculative abstractions for future server runtimes unless explicitly requested
